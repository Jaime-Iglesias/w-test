import { emojify } from "react-emojione";

const text = {
  title: emojify("ETH :left_right_arrow: WETH"),
  wrap: "Wrap",
  eth: "ETH",
  unwrap: "Unwrap",
  weth: "WETH",
  instructions: "Wrap ETH into an ERC20-compliant Ether token. 1 ETH = 1 WETH*"
};

export default text;
